server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Railway's internal DNS resolver
    resolver 127.0.0.11 valid=30s;

    # Serve static frontend files.
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Proxy API calls to backend container.
    location /api/ {
        set $backend_service "dtdm-nhom3.railway.internal";
        proxy_pass http://$backend_service:8000/api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Proxy OAuth login and callback endpoints.
    location /login/ {
        set $backend_service "dtdm-nhom3.railway.internal";
        proxy_pass http://$backend_service:8000/login/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /auth/ {
        set $backend_service "dtdm-nhom3.railway.internal";
        proxy_pass http://$backend_service:8000/auth/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Optional backend docs through same origin.
    location /docs {
        set $backend_service "dtdm-nhom3.railway.internal";
        proxy_pass http://$backend_service:8000/docs;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /openapi.json {
        set $backend_service "dtdm-nhom3.railway.internal";
        proxy_pass http://$backend_service:8000/openapi.json;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
