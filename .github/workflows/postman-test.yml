name: Automation Testing with Postman CLI

on:
  push:
    branches: [ "main", "master", "develop" ]
  pull_request:
    branches: [ "main" ]

jobs:
  run-postman-tests:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Install Postman CLI
      run: |
        curl -o- "https://dl-cli.pstmn.io/install/linux64.sh" | sh

    - name: Login to Postman CLI
      run: |
        postman login --with-api-key ${{ secrets.POSTMAN_API_KEY }}

    - name: Run Postman Collection
      run: |
        postman collection run ${{ secrets.POSTMAN_COLLECTION_ID }}

    # TỰ ĐỘNG TẠO BUG JIRA BẰNG ACTION TÍCH HỢP (KHÔNG CẦN BƯỚC LOGIN RIÊNG)
    - name: Create Jira Issue on Failure
      if: failure()
      uses: bradenwright/jira-create-issue-action@v1
      with:
        domain: 'ktpm-uth.atlassian.net'
        user-email: ${{ secrets.JIRA_USER }}
        api-token: ${{ secrets.JIRA_TOKEN }}
        project: 'KAN'
        issue-type: 'Bug'
        summary: '🔴 [Automation Test] Phát hiện lỗi API khi chạy Automation Test Pipeline'
        description: |
          Hệ thống kiểm thử tự động phát hiện có API bị lỗi.
          Vui lòng kiểm tra lại log chi tiết trên GitHub Actions.
          Luồng kiểm thử: Postman CLI.
